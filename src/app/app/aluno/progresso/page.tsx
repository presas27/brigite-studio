import { redirect } from "next/navigation";

/** Folded into Evolução. Kept so an old bookmark still lands somewhere. */
export default function AlunoProgressoPage() {
  redirect("/app/aluno/evolucao");
}
